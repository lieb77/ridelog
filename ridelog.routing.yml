ridelog.montly:
  path: '/bike/ridelog/montly'
  defaults:
    _title: 'Ridelog'
    _controller: '\Drupal\ridelog\Controller\RidelogController:montly'
  requirements:
    _permission: 'access content'
  options:
    no_cache: 'TRUE'

ridelog.yearly:
  path: '/bike/ridelog/yearly'
  defaults:
    _title: 'Ridelog'
    _controller: '\Drupal\ridelog\Controller\RidelogController:yearly'
  requirements:
    _permission: 'access content'
  options:
    no_cache: 'TRUE'

