services:
  logger.channel.ridelog:
    parent: logger.channel_base
    arguments: ['ridelog']

  ridelog.rides:
    class: Drupal\ridelog\Rides

  ridelog.ridelog:
    class: Drupal\ridelog\RideLog
    arguments: ['@entity_type.manager', '@logger.channel.ridelog', '@ridelog.rides']

